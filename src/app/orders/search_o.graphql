query orders_s($name:String!){
  allOrder(searchByName:$name){
    edges{
      node{
        _id
        id
        status
        expectedDate
        relatedService{
          relatedPatient{
            relatedProfile{
              firstName
              profilePic
            }
          }
        }
      }
    }
  }
}
