query orders_state($name:String!){
  allOrder(statusStartwith:$name){
    edges{
      node{
        _id
        id
        status
        expectedDate
        updatedAt
        relatedService{
          relatedPatient{
            relatedProfile{
              firstName
              profilePic
            }
          }
        }
      }
    }
  }
}
