query orders($dr:[String]){
  allOrder(doctorId:$dr){
    edges{
      node{
        _id
        id
        status
        expectedDate
        updatedAt
        relatedService{
          relatedPatient{
            relatedProfile{
              firstName
              profilePic
            }
          }
        }
      }
    }
  }
}
